# Test & development dependencies for the pipeline
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.25,<1.0
pytest-cov>=6.0,<7.0
respx>=0.22,<1.0       # httpx mocking (like responses, but for httpx)
factory-boy>=3.3,<4.0   # test factories for SQLAlchemy models

# Linting / formatting
ruff>=0.9,<1.0
