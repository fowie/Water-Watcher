# Utah — Round 7

**Time:** 2026-02-24T16:10
**Role:** Backend
**Mode:** Background

## Task
Build BLM + USFS scrapers, user profile API, wire land agency scrapers on 6-hour schedule.

## Output
- Created BLM scraper (`pipeline/scrapers/blm.py`): BLM recreation API + RSS feed, advisory type/severity classification, river name regex extraction, 2s rate limiting.
- Created USFS scraper (`pipeline/scrapers/usfs.py`): RIDB API with `RIDB_API_KEY`, facility + rec area alerts, graceful skip when key absent, 1s rate limiting.
- New scheduled job `run_land_agency_scrapers()` on 6-hour interval (configurable via `LAND_AGENCY_INTERVAL_MINUTES`).
- Added settings: `blm_base_url`, `ridb_api_key`, `land_agency_interval_minutes`.
- Created `GET/PATCH /api/user/profile` with `withAuth()`, duplicate-email checking (409), riverCount/filterCount via `_count`.
- Updated `web/src/lib/api.ts` with `getUserProfile()`, `updateUserProfile()`.
- Updated `.env.example`, `pipeline/scrapers/__init__.py`, `test_main.py` assertions.

## Decisions
BD-019
