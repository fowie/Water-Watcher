# Tyler — Round 6

**Time:** 2026-02-24T16:00
**Role:** Frontend
**Mode:** Background

## Task
Build auth UI — sign-in, registration, session-aware navigation and route protection.

## Output
- Sign-in page (`/auth/signin`) — full-screen centered card, no navigation chrome.
- Registration page (`/auth/register`) — Zod client-side validation, auto-sign-in on success.
- `UserMenu` component — authenticated user display in sidebar (desktop) and header (mobile).
- `AuthGuard` component — `useSession()` + `useEffect` redirect pattern, skeleton during loading.
- `SessionProvider` wrapping entire app in root `layout.tsx`.
- Navigation updated: Settings link conditional on auth state.
- Settings page uses `session.user.id` instead of `DEMO_USER_ID`.
- 15 routes, build clean.

## Decisions
FE-009
