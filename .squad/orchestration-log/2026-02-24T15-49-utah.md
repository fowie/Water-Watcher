# Utah — Round 5

**Time:** 2026-02-24T15:49
**Role:** Backend / DevOps
**Mode:** Background

## Task
Production readiness: Docker setup, CI workflow, comprehensive README.

## Output
- `web/Dockerfile` — multi-stage Node 20 Alpine build (deps → builder → runner), pnpm, standalone output, non-root user.
- `pipeline/Dockerfile` — Python 3.12-slim, system deps for psycopg2/lxml, Playwright chromium.
- `docker-compose.yml` — expanded to 4 services (postgres, db-migrate, web, pipeline) with health checks and dependency ordering.
- `.github/workflows/ci.yml` — 4 parallel jobs (web-test, web-build, web-lint, pipeline-test) with caching.
- `README.md` — full rewrite with Docker quick start, local dev setup, API endpoint table, architecture diagram.
- `.env.example` — updated with Docker hostname guidance.
- `.dockerignore` files for web and pipeline.
- `web/next.config.ts` — added `output: "standalone"`.

## Decisions
BD-015, BD-016, BD-017
