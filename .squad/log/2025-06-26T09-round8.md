# Session Log — Round 8

**Date:** 2026-02-24  
**Round:** 8

## Summary

Round 8 added email notifications, OAuth sign-in, notification preferences, and alert history across both backend and frontend.

**Backend (Utah):** `EmailNotifier` class using Resend API with 4 alert types (deal, condition, hazard, weekly digest). Email dispatch gated by per-user `NotificationPreference` model (channel + per-type toggles). `AlertLog` model tracks all sent notifications. Google and GitHub OAuth providers added to NextAuth. New API routes for notification preferences and alert history.

**Frontend (Tyler):** OAuth buttons (Google/GitHub) on sign-in and registration pages. Notification preferences section on Settings page with channel selector and toggle switches. Alert history page at `/alerts` with filter tabs and pagination. `NotificationBell` component in navigation with unread count and dropdown.

**Testing (Pappas):** 116 new tests covering email notifier (70), notification prefs API (22), and alerts API (24). Total test count: 1023 + 43 skipped. Discovered `limit=0` falsy edge case in alerts route.

**Coordinator:** Fixed alerts API `limit=0` edge case — `Number.isFinite` instead of `||` fallback.

## Metrics

| Service  | Passed | Skipped | Total |
|----------|--------|---------|-------|
| Pipeline | 636    | 43      | 679   |
| Web      | 387    | 0       | 387   |
| **All**  | **1023** | **43** | **1066** |

## Commits

- `f538d0d` — Round 8 features (email notifications, OAuth, notification prefs, alert history, tests)
- `4570fc5` — fix: alerts API limit=0 treated as falsy, use Number.isFinite instead
